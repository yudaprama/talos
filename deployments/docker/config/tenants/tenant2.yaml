# Tenant 2 configuration
secrets:
  default:
    current: "docker-compose-tenant2-secret-32-chars-long"
    retired: []
  pagination:
    current: "docker-compose-tenant2-pagination-secret-32ch"
    retired: []

# Signing keys for token derivation (JWT and macaroon)
credentials:
  derived_tokens:
    jwt:
      signing_keys:
        urls:
          - "file:///etc/talos/jwks.json"
