# Agent Handbook for Ory Talos

## 📦 Project overview
- **Language**: Go 1.26 (module `github.com/ory/talos`).
- **Purpose**: High‑performance API‑key service with admin & self‑service APIs, JWT/macaroons derivation, multi‑DB support.
- **Key packages**:
  - `cmd/` – CLI entry points (`talos`, sub‑commands).
  - `internal/` – core implementation, persistence, crypto, tracing, metrics.
  - `pkg/` – public SDKs & helper libraries.
  - `commercial/` – enterprise‑only code guarded by build tags.
  - `oryx/` – vendored Ory‑X utilities (replace directive in `go.mod`).

## 🛠️ Development workflow (as documented in `CONTRIBUTING.md`)
1. Fork → create branch `feat/<name>`.
2. Run **`make verify`** after each change (executes lint, fmt, tests, docs).
3. Commit using Conventional Commits.
4. Push and open a PR against `first-implementation`.

## 🚀 Build & run commands (Makefile targets)
| Target | Description |
|--------|-------------|
| `make deps` | Install Go tools (`buf`, `sqlc`, `golangci-lint`, etc.). |
| `make build` | Build OSS binary `talos` (`CGO_ENABLED=0`). |
| `make build-commercial` | Build commercial binary (`talos-commercial`). |
| `make test` | Run all unit & integration tests (spins up Docker DBs). |
| `make test-db-start` | Start Docker Compose test DBs (`docker-compose.test.yml`). |
| `make migrate` | Apply DB migrations (`cmd/migrate`). |
| `make generate` | Generate protobuf, SQL, OpenAPI, SDK client, docs, events. |
| `make fmt` | Run `gofumpt`, `goimports`, `prettier` on all sources. |
| `make lint` | Run `golangci-lint` with the repo config. |
| `make verify` | Alias for `fmt && lint && test && docs-test`. |

## 📚 Documentation & SDK generation
- **OpenAPI**: `api/talos.openapi-v3.json` is generated by `make generate-openapi` (via `buf` and Ory tooling).
- **Go client SDK**: `internal/client/generated` – regenerated by `make generate`.
- **Docs**: `make docs-test` validates markdown examples, `make generate` syncs code blocks.

## 🗄️ Persistence layer (core contract)
- Interface `persistence.Persister` (see `internal/persistence/persistence.go`).
- **Network ID (NID)** handling:
  - OSS → `uuid.Nil` (single‑tenant).
  - Commercial → `contextx.RequiredNetworkIDFromContext` – fails loudly if missing.
- Drivers live under `internal/persistence/sqlc/generated` (SQLite, Postgres, MySQL, Cockroach).  
- **Gotcha**: background workers must inject NID into the context before calling any persister method.

## 🔧 Code conventions (enforced by CI & lint config)
- No global state – all dependencies are injected.
- Always pass `context.Context` from the caller.
- Structured JSON logging via `slog` (see `internal/logger`).
- Use the injected `*retryablehttp.Client` – direct `http.Get/Do` is forbidden (`golangci-lint` `forbidigo`).
- Avoid `COUNT(*)`, `SELECT *`, and `OFFSET`; use explicit column lists and cursor pagination.
- Commercial code must be under `/commercial/` and guarded with `//go:build commercial`.
- Tests must be table‑driven, parallel where safe, and cover happy & error paths (`make test` expects ≥85% coverage).

## 🧪 Testing setup
- **Docker Compose** files:
  - `docker-compose.oss.yaml` – SQLite + optional DB containers for OSS.
  - `docker-compose.test.yml` – PostgreSQL, MySQL, Cockroach test DBs.
- Run `make test-db-start` before `make test` if you need to inspect the DB.
- Integration tests live under `test/` (E2E) and use the same Docker Compose stack.
- Lint exclusions exist for generated protobuf (`*.pb.go`) and test helper patterns – respect them.

## 📦 CI / GitHub Actions
- `build.yml` is the **only** workflow. It cross‑compiles `talos` for `linux`/`darwin` (amd64 + arm64) with `CGO_ENABLED=0` on a single `ubuntu-latest` runner — no macOS runners, no cgo, no secrets.
- A push to `master` runs the 4 build jobs only (verifies the build stays green); **no release** is published.
- Pushing a `v*` tag runs the same builds and then publishes a GitHub Release with all four binaries (see **Releasing** below).
- The upstream Ory `ci.yaml` and `.github/actions/talos/` were **removed** — they required commercial OpenAPI patches and Ory‑internal secrets and could never pass on this fork.
- `gh` resolves to the **upstream** `ory/talos` by default; always pass `-R yudaprama/talos` (or run `gh repo set-default yudaprama/talos`).

## 🚀 Releasing (how to publish a new version)

Releases are **tag‑driven and fully automatic**. The CI workflow builds the binaries and attaches them to a GitHub Release — there is **no** manual `gh release` / `goreleaser` step.

> **This fork commits directly to `master` (no feature branches, no PRs)** — the upstream "Fork → branch → PR" workflow in the Development section above does **not** apply here.

To release version `vX.Y.Z` (SemVer, `v` prefix required):

1. Confirm every change is committed and pushed to `master`, and the automatic master build passed:
   ```bash
   gh run list -R yudaprama/talos --branch master --workflow build.yml --limit 1
   ```
2. Tag the `master` HEAD and push the tag. **Tag *after* pushing master** — a tag‑triggered workflow uses the workflow file present at the tagged commit:
   ```bash
   git tag -a vX.Y.Z -m "vX.Y.Z"
   git push origin vX.Y.Z
   ```
3. Watch the tag build + release job until it completes green:
   ```bash
   RUN=$(gh run list -R yudaprama/talos --branch vX.Y.Z --workflow build.yml --limit 1 \
         --json databaseId -q '.[0].databaseId')
   gh run watch "$RUN" -R yudaprama/talos --exit-status
   ```
4. Verify the release and its four assets:
   ```bash
   gh release view vX.Y.Z -R yudaprama/talos
   ```

Result: a release at `https://github.com/yudaprama/talos/releases/tag/vX.Y.Z` (marked **Latest**) with `talos-{linux,darwin}-{amd64,arm64}`.

Rules & gotchas:
- `X.Y.Z` must follow SemVer and be **new** — reusing a tag fails the push. Never move or delete an already‑published tag.
- Only `v*` tags publish a release. Branch pushes and `workflow_dispatch` build but do **not** release.
- No secrets are needed — the workflow uses the built‑in `GITHUB_TOKEN` (`permissions: contents: write`).
- Version metadata is injected via `-ldflags` into `internal/version` (`Version`, `Commit`, `BuildTime`); the version string is the tag with its leading `v` stripped (tag `v0.1.0` → `0.1.0`).
- To change which platforms/architectures ship, edit the `matrix` in `.github/workflows/build.yml`.
- Fork versioning starts at **`v0.1.0`** (2026‑06‑25). The `v26.x`/`v27.x` tags are upstream artifacts, not this fork's releases.

## ⚠️ Gotchas & non‑obvious patterns
1. **Network ID injection** – commercial code crashes if the NID is missing; remember to call `contextx.WithNetworkID(ctx, nid)` in background workers.
2. **Build tags** – the `commercial/` folder is excluded from OSS builds; adding a file there without `//go:build commercial` will break `make build`.
3. **HTTP client** – direct `http.Get/Post` triggers a `forbidigo` lint error; always obtain the client via `internal/client.NewClient()` which returns a retryable client.
4. **SQL generation** – `sqlc` runs on both `internal/persistence/sqlc` (OSS) and `commercial/persistence/sqlc` (enterprise). Changing a schema requires running `make generate` to keep both in sync.
5. **Protobuf generation** – `buf` excludes generated files with `// Code generated by protoc` comments; never edit those files directly.
6. **Lint config** – many linters are disabled for readability (e.g., `lll`, `varnamelen`). Do not enable them globally without a strong reason.
7. **Testing** – tests deliberately ignore `COUNT(*)` style queries; the linter `gosec` excludes false‑positives for DB calls.

## 📦 Useful make shortcuts
```
make deps           # install all required tools
make generate      # regenerate protobuf, sqlc, OpenAPI, docs, SDK
make fmt           # format all sources
make lint          # run linters
make test           # run unit & integration tests (needs DB containers)
make verify        # fmt + lint + test + docs-test (full CI check)
make build          # build OSS binary
make build-commercial # build enterprise binary
```

---
*Generated with Crush – this file reflects the actual state of the repository as of the latest inspection.*